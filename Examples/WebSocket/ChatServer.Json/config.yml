altruist:
  server:
    http:
      host: "0.0.0.0"
      port: 8080
      path: "/"
    transport:
      mode: websocket
      codec:
        provider: json

  persistence:
    cache:
      provider: inmemory
